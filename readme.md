Setup

Install dependencies:

npm install

Create .env:

OPENROUTER_API_KEY=your_key_here

Run:

node generate_scripts.js 0 5 (ví dụ chạy từ trang 0 đến trang 5)

Output:

scripts.json
Models

Current default:

google/gemini-2.5-flash
